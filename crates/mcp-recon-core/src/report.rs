//! Markdown report renderer.
//!
//! Takes an inventory + fuzz results + classification and renders the
//! single-document threat profile a security reviewer reads. The
//! companion JSON document carries the same data structurally for
//! downstream tooling (capnagent's caveat-suggestion bridge).
//!
//! v0.1 status: scaffold. The renderer lands in week 3 alongside the
//! classifier.

// Stub. v0.1 week 3 will populate.
