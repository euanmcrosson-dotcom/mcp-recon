{
  "name": "@mcp-recon/cli",
  "version": "0.2.0",
  "description": "Reverse-engineer MCP server tool surfaces. Enumerate, fuzz, classify, report. CLI + library.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "mcp-recon": "./src/bin/recon.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc",
    "lint": "biome check src",
    "typecheck": "tsc --noEmit",
    "recon": "tsx src/bin/recon.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "25.6.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "tsx": "^4.21.0",
    "typescript": "5.6.3",
    "vitest": "^2.1.9"
  }
}
