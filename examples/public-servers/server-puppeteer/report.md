# Threat profile: example-servers/puppeteer v0.1.0

> Generated by [mcp-recon](https://github.com/euanmcrosson-dotcom/mcp-recon) at 2026-05-01T01:28:43.378Z.
> Companion to [capnagent](https://github.com/euanmcrosson-dotcom/capnagent) — every recommended caveat below is a copy-pasteable capnagent predicate.

## Summary

- **Tools:** 7
- **Data-class distribution:** messaging (4), network (1), unknown (1), shell (1)
- **Authority-level distribution:** write (4), read (2), privileged (1)
- **Confused-deputy candidates:** 5 / 7
- **Fuzz:** 357 calls — ok=64, protocol_error=187, runtime_error=106  ⚠️ at least one input escaped protocol-error handling

## Tools

### puppeteer_evaluate

**Class:** `shell` · **Authority:** `privileged` ⚠️ confused-deputy candidate · **Confidence:** 55.0%

**Description:** Execute JavaScript in the browser console

**Rationale:** description match "\b(execute|shell|subprocess|invoke|launch[_-]?process)\b" → shell/privileged (0.50); fuzz: 3/30 accepted → +0.1; user-controllable string arg + non-read authority → confused-deputy candidate

**Fuzz:** 30 calls — ok=3, protocol_error=27, runtime_error=0

**Recommended capnagent caveat:**

```
tool != "puppeteer_evaluate"  // PRIVILEGED — recommend deny outright; operator should hand-write argv allowlist if exposing
```

### puppeteer_click

**Class:** `messaging` · **Authority:** `write` ⚠️ confused-deputy candidate · **Confidence:** 50.0%

**Description:** Click an element on the page

**Rationale:** description match "\b(email|notification|message|chat|page|alert)\b" → messaging/read (0.50); description has side-effect verb → escalate one step; user-controllable string arg + non-read authority → confused-deputy candidate

**Fuzz:** 30 calls — ok=0, protocol_error=29, runtime_error=1  ⚠️

**Recommended capnagent caveat:**

```
tool == "puppeteer_click" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // WRITE messaging; bound recipient list + caller
```

### puppeteer_hover

**Class:** `messaging` · **Authority:** `write` ⚠️ confused-deputy candidate · **Confidence:** 50.0%

**Description:** Hover an element on the page

**Rationale:** description match "\b(email|notification|message|chat|page|alert)\b" → messaging/read (0.50); description has side-effect verb → escalate one step; user-controllable string arg + non-read authority → confused-deputy candidate

**Fuzz:** 30 calls — ok=0, protocol_error=12, runtime_error=18  ⚠️

**Recommended capnagent caveat:**

```
tool == "puppeteer_hover" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // WRITE messaging; bound recipient list + caller
```

### puppeteer_screenshot

**Class:** `messaging` · **Authority:** `write` ⚠️ confused-deputy candidate · **Confidence:** 55.0%

**Description:** Take a screenshot of the current page or a specific element

**Rationale:** description match "\b(email|notification|message|chat|page|alert)\b" → messaging/read (0.50); fuzz: 58/94 accepted → +0.1; description has side-effect verb → escalate one step; user-controllable string arg + non-read authority → confused-deputy candidate

**Fuzz:** 94 calls — ok=58, protocol_error=35, runtime_error=1  ⚠️

**Recommended capnagent caveat:**

```
tool == "puppeteer_screenshot" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // WRITE messaging; bound recipient list + caller
```

### puppeteer_select

**Class:** `messaging` · **Authority:** `write` ⚠️ confused-deputy candidate · **Confidence:** 50.0%

**Description:** Select an element on the page with Select tag

**Rationale:** description match "\b(email|notification|message|chat|page|alert)\b" → messaging/read (0.50); description has side-effect verb → escalate one step; user-controllable string arg + non-read authority → confused-deputy candidate

**Fuzz:** 55 calls — ok=0, protocol_error=12, runtime_error=43  ⚠️

**Recommended capnagent caveat:**

```
tool == "puppeteer_select" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // WRITE messaging; bound recipient list + caller
```

### puppeteer_navigate

**Class:** `network` · **Authority:** `read` · **Confidence:** 73.0%

**Description:** Navigate to a URL

**Rationale:** description match "\b(http|https|fetch|request|webhook|api[_-]?call|url|origin)\b" → network/read (0.50); schema: arg "url" is URL-shaped → network (0.40); fuzz: 3/63 accepted → +0.1

**Fuzz:** 63 calls — ok=3, protocol_error=60, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "puppeteer_navigate" AND caller == "<your-caller-id>" AND arg.url == "https://<your-allowed-origin>" AND now <= @<your-cap-expiry>  // READ network; allowlist exact origins (capnagent round 05/09)
```

### puppeteer_fill

**Class:** `unknown` · **Authority:** `read` · **Confidence:** 0.0%

**Description:** Fill out an input field

**Rationale:** no rules fired

**Fuzz:** 55 calls — ok=0, protocol_error=12, runtime_error=43  ⚠️

**Recommended capnagent caveat:**

```
tool == "puppeteer_fill" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ unclassified — operator must review
```

---

## How to read this report

Every classification has a `confidence` score (noisy-OR over fired rules — see [docs/METHODOLOGY.md](https://github.com/euanmcrosson-dotcom/mcp-recon/blob/master/docs/METHODOLOGY.md)) and a free-form `rationale` listing every rule that fired. If you disagree with a classification:

1. Read the `rationale` — which rules fired, and which you'd reweight.
2. Edit `crates/mcp-recon-core/src/rules/v0_1.rs` (rule weights) or `packages/mcp-recon-cli/src/classify/rules.ts` (TS mirror) and re-run.
3. Open an issue with your reasoning so future runs benefit.

**Recommended caveats are starting points, not finish lines.** Tighten the placeholders to match your deployment, and consider attaching additional caveats (caller binding, time-of-day windows, rate limits enforced upstream).
