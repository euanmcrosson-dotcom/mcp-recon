# Threat profile: secure-filesystem-server v0.2.0

> Generated by [mcp-recon](https://github.com/euanmcrosson-dotcom/mcp-recon) at 2026-04-29T22:35:27.187Z.
> Companion to [capnagent](https://github.com/euanmcrosson-dotcom/capnagent) — every recommended caveat below is a copy-pasteable capnagent predicate.

## Summary

- **Tools:** 14
- **Data-class distribution:** filesystem (14)
- **Authority-level distribution:** read (10), write (4)
- **Confused-deputy candidates:** 4 / 14
- **Fuzz:** 723 calls — ok=4, protocol_error=719, runtime_error=0

## Tools

### create_directory

**Class:** `filesystem` · **Authority:** `write` ⚠️ confused-deputy candidate · **Confidence:** 91.0%

**Description:** Create a new directory or ensure a directory exists. Can create multiple nested directories in one operation. If the directory already exists, this operation will succeed silently. Perfect for setting up directory structures for projects or ensuring required paths exist. Only works within allowed directories.

**Rationale:** name match "\b(write[_-]?file|edit[_-]?file|create[_-]?directory|move[_-]?file)\b" → filesystem/write (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "path" is path-shaped → filesystem (0.40); user-controllable string arg + non-read authority → confused-deputy candidate

**Fuzz:** 41 calls — ok=0, protocol_error=41, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "create_directory" AND caller == "<your-caller-id>" AND arg.path starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // WRITE filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### edit_file

**Class:** `filesystem` · **Authority:** `write` ⚠️ confused-deputy candidate · **Confidence:** 91.0%

**Description:** Make line-based edits to a text file. Each edit replaces exact line sequences with new content. Returns a git-style diff showing the changes made. Only works within allowed directories.

**Rationale:** name match "\b(write[_-]?file|edit[_-]?file|create[_-]?directory|move[_-]?file)\b" → filesystem/write (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "path" is path-shaped → filesystem (0.40); user-controllable string arg + non-read authority → confused-deputy candidate

**Fuzz:** 61 calls — ok=0, protocol_error=61, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "edit_file" AND caller == "<your-caller-id>" AND arg.path starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // WRITE filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### move_file

**Class:** `filesystem` · **Authority:** `write` ⚠️ confused-deputy candidate · **Confidence:** 94.6%

**Description:** Move or rename files and directories. Can move files between directories and rename them in a single operation. If the destination exists, the operation will fail. Works across different directories and can be used for simple renaming within the same directory. Both source and destination must be within allowed directo…

**Rationale:** name match "\b(write[_-]?file|edit[_-]?file|create[_-]?directory|move[_-]?file)\b" → filesystem/write (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "source" is path-shaped → filesystem (0.40); schema: arg "destination" is path-shaped → filesystem (0.40); user-controllable string arg + non-read authority → confused-deputy candidate

**Fuzz:** 77 calls — ok=0, protocol_error=77, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "move_file" AND caller == "<your-caller-id>" AND arg.source starts_with "<your-sandbox-prefix>/" AND arg.destination starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // WRITE filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### write_file

**Class:** `filesystem` · **Authority:** `write` ⚠️ confused-deputy candidate · **Confidence:** 91.0%

**Description:** Create a new file or completely overwrite an existing file with new content. Use with caution as it will overwrite existing files without warning. Handles text content with proper encoding. Only works within allowed directories.

**Rationale:** name match "\b(write[_-]?file|edit[_-]?file|create[_-]?directory|move[_-]?file)\b" → filesystem/write (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "path" is path-shaped → filesystem (0.40); user-controllable string arg + non-read authority → confused-deputy candidate

**Fuzz:** 66 calls — ok=0, protocol_error=66, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "write_file" AND caller == "<your-caller-id>" AND arg.path starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // WRITE filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### directory_tree

**Class:** `filesystem` · **Authority:** `read` · **Confidence:** 91.0%

**Description:** Get a recursive tree view of files and directories as a JSON structure. Each entry includes 'name', 'type' (file/directory), and 'children' for directories. Files have no children array, while directories always have a children array (which may be empty). The output is formatted with 2-space indentation for readability…

**Rationale:** name match "\b(list[_-]?(directory|directory[_-]?with[_-]?sizes)|directory[_-]?tree)\b" → filesystem/read (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "path" is path-shaped → filesystem (0.40)

**Fuzz:** 50 calls — ok=0, protocol_error=50, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "directory_tree" AND caller == "<your-caller-id>" AND arg.path starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // READ filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### get_file_info

**Class:** `filesystem` · **Authority:** `read` · **Confidence:** 91.0%

**Description:** Retrieve detailed metadata about a file or directory. Returns comprehensive information including size, creation time, last modified time, permissions, and type. This tool is perfect for understanding file characteristics without reading the actual content. Only works within allowed directories.

**Rationale:** name match "\b(get[_-]?file[_-]?info|search[_-]?files|list[_-]?allowed[_-]?directories)\b" → filesystem/read (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "path" is path-shaped → filesystem (0.40)

**Fuzz:** 41 calls — ok=0, protocol_error=41, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "get_file_info" AND caller == "<your-caller-id>" AND arg.path starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // READ filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### list_allowed_directories

**Class:** `filesystem` · **Authority:** `read` · **Confidence:** 73.0%

**Description:** Returns the list of directories that this server is allowed to access. Subdirectories within these allowed directories are also accessible. Use this to understand which directories and their nested paths are available before trying to access files.

**Rationale:** name match "\b(get[_-]?file[_-]?info|search[_-]?files|list[_-]?allowed[_-]?directories)\b" → filesystem/read (0.70); fuzz: 4/4 accepted → +0.1

**Fuzz:** 4 calls — ok=4, protocol_error=0, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "list_allowed_directories" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### list_directory

**Class:** `filesystem` · **Authority:** `read` · **Confidence:** 91.0%

**Description:** Get a detailed listing of all files and directories in a specified path. Results clearly distinguish between files and directories with [FILE] and [DIR] prefixes. This tool is essential for understanding directory structure and finding specific files within a directory. Only works within allowed directories.

**Rationale:** name match "\b(list[_-]?(directory|directory[_-]?with[_-]?sizes)|directory[_-]?tree)\b" → filesystem/read (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "path" is path-shaped → filesystem (0.40)

**Fuzz:** 41 calls — ok=0, protocol_error=41, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "list_directory" AND caller == "<your-caller-id>" AND arg.path starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // READ filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### list_directory_with_sizes

**Class:** `filesystem` · **Authority:** `read` · **Confidence:** 91.0%

**Description:** Get a detailed listing of all files and directories in a specified path, including sizes. Results clearly distinguish between files and directories with [FILE] and [DIR] prefixes. This tool is useful for understanding directory structure and finding specific files within a directory. Only works within allowed directori…

**Rationale:** name match "\b(list[_-]?(directory|directory[_-]?with[_-]?sizes)|directory[_-]?tree)\b" → filesystem/read (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "path" is path-shaped → filesystem (0.40)

**Fuzz:** 64 calls — ok=0, protocol_error=64, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "list_directory_with_sizes" AND caller == "<your-caller-id>" AND arg.path starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // READ filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### read_file

**Class:** `filesystem` · **Authority:** `read` · **Confidence:** 91.0%

**Description:** Read the complete contents of a file as text. DEPRECATED: Use read_text_file instead.

**Rationale:** name match "\b(read[_-]?(file|text[_-]?file|media[_-]?file|multiple[_-]?files))\b" → filesystem/read (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "path" is path-shaped → filesystem (0.40)

**Fuzz:** 73 calls — ok=0, protocol_error=73, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "read_file" AND caller == "<your-caller-id>" AND arg.path starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // READ filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### read_media_file

**Class:** `filesystem` · **Authority:** `read` · **Confidence:** 91.0%

**Description:** Read an image or audio file. Returns the base64 encoded data and MIME type. Only works within allowed directories.

**Rationale:** name match "\b(read[_-]?(file|text[_-]?file|media[_-]?file|multiple[_-]?files))\b" → filesystem/read (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "path" is path-shaped → filesystem (0.40)

**Fuzz:** 41 calls — ok=0, protocol_error=41, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "read_media_file" AND caller == "<your-caller-id>" AND arg.path starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // READ filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### read_multiple_files

**Class:** `filesystem` · **Authority:** `read` · **Confidence:** 91.0%

**Description:** Read the contents of multiple files simultaneously. This is more efficient than reading files one by one when you need to analyze or compare multiple files. Each file's content is returned with its path as a reference. Failed reads for individual files won't stop the entire operation. Only works within allowed director…

**Rationale:** name match "\b(read[_-]?(file|text[_-]?file|media[_-]?file|multiple[_-]?files))\b" → filesystem/read (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "paths" is path-shaped → filesystem (0.40)

**Fuzz:** 16 calls — ok=0, protocol_error=16, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "read_multiple_files" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### read_text_file

**Class:** `filesystem` · **Authority:** `read` · **Confidence:** 91.0%

**Description:** Read the complete contents of a file from the file system as text. Handles various text encodings and provides detailed error messages if the file cannot be read. Use this tool when you need to examine the contents of a single file. Use the 'head' parameter to read only the first N lines of a file, or the 'tail' parame…

**Rationale:** name match "\b(read[_-]?(file|text[_-]?file|media[_-]?file|multiple[_-]?files))\b" → filesystem/read (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "path" is path-shaped → filesystem (0.40)

**Fuzz:** 73 calls — ok=0, protocol_error=73, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "read_text_file" AND caller == "<your-caller-id>" AND arg.path starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // READ filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### search_files

**Class:** `filesystem` · **Authority:** `read` · **Confidence:** 91.0%

**Description:** Recursively search for files and directories matching a pattern. The patterns should be glob-style patterns that match paths relative to the working directory. Use pattern like '*.ext' to match files in current directory, and '**/*.ext' to match files in all subdirectories. Returns full paths to all matching items. Gre…

**Rationale:** name match "\b(get[_-]?file[_-]?info|search[_-]?files|list[_-]?allowed[_-]?directories)\b" → filesystem/read (0.70); description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); schema: arg "path" is path-shaped → filesystem (0.40)

**Fuzz:** 75 calls — ok=0, protocol_error=75, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "search_files" AND caller == "<your-caller-id>" AND arg.path starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // READ filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

---

## How to read this report

Every classification has a `confidence` score (noisy-OR over fired rules — see [docs/METHODOLOGY.md](https://github.com/euanmcrosson-dotcom/mcp-recon/blob/master/docs/METHODOLOGY.md)) and a free-form `rationale` listing every rule that fired. If you disagree with a classification:

1. Read the `rationale` — which rules fired, and which you'd reweight.
2. Edit `crates/mcp-recon-core/src/rules/v0_1.rs` (rule weights) or `packages/mcp-recon-cli/src/classify/rules.ts` (TS mirror) and re-run.
3. Open an issue with your reasoning so future runs benefit.

**Recommended caveats are starting points, not finish lines.** Tighten the placeholders to match your deployment, and consider attaching additional caveats (caller binding, time-of-day windows, rate limits enforced upstream).
