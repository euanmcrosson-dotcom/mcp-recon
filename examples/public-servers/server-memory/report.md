# Threat profile: memory-server v0.6.3

> Generated by [mcp-recon](https://github.com/euanmcrosson-dotcom/mcp-recon) at 2026-04-29T21:25:54.831Z.
> Companion to [capnagent](https://github.com/euanmcrosson-dotcom/capnagent) — every recommended caveat below is a copy-pasteable capnagent predicate.

## Summary

- **Tools:** 9
- **Data-class distribution:** metadata (9)
- **Authority-level distribution:** write (3), destructive (3), read (3)
- **Confused-deputy candidates:** 0 / 9 ✅
- **Fuzz:** 124 calls — ok=18, protocol_error=106, runtime_error=0

## Tools

### delete_entities

**Class:** `metadata` · **Authority:** `destructive` · **Confidence:** 73.0%

**Description:** Delete multiple entities and their associated relations from the knowledge graph

**Rationale:** name match "\b(delete[_-]?(entities|relations|observations))\b" → metadata/destructive (0.70); fuzz: 1/15 accepted → +0.1; description has destructive verb → escalate to destructive

**Fuzz:** 15 calls — ok=1, protocol_error=14, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "delete_entities" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>
```

### delete_observations

**Class:** `metadata` · **Authority:** `destructive` · **Confidence:** 73.0%

**Description:** Delete specific observations from entities in the knowledge graph

**Rationale:** name match "\b(delete[_-]?(entities|relations|observations))\b" → metadata/destructive (0.70); fuzz: 1/15 accepted → +0.1; description has destructive verb → escalate to destructive

**Fuzz:** 15 calls — ok=1, protocol_error=14, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "delete_observations" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>
```

### delete_relations

**Class:** `metadata` · **Authority:** `destructive` · **Confidence:** 73.0%

**Description:** Delete multiple relations from the knowledge graph

**Rationale:** name match "\b(delete[_-]?(entities|relations|observations))\b" → metadata/destructive (0.70); fuzz: 1/15 accepted → +0.1; description has destructive verb → escalate to destructive

**Fuzz:** 15 calls — ok=1, protocol_error=14, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "delete_relations" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>
```

### add_observations

**Class:** `metadata` · **Authority:** `write` · **Confidence:** 73.0%

**Description:** Add new observations to existing entities in the knowledge graph

**Rationale:** name match "\b(add[_-]?observations)\b" → metadata/write (0.70); fuzz: 1/15 accepted → +0.1

**Fuzz:** 15 calls — ok=1, protocol_error=14, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "add_observations" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // WRITE metadata
```

### create_entities

**Class:** `metadata` · **Authority:** `write` · **Confidence:** 73.0%

**Description:** Create multiple new entities in the knowledge graph

**Rationale:** name match "\b(create[_-]?(entities|relations))\b" → metadata/write (0.70); fuzz: 1/15 accepted → +0.1

**Fuzz:** 15 calls — ok=1, protocol_error=14, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "create_entities" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // WRITE metadata
```

### create_relations

**Class:** `metadata` · **Authority:** `write` · **Confidence:** 73.0%

**Description:** Create multiple new relations between entities in the knowledge graph. Relations should be in active voice

**Rationale:** name match "\b(create[_-]?(entities|relations))\b" → metadata/write (0.70); fuzz: 1/15 accepted → +0.1

**Fuzz:** 15 calls — ok=1, protocol_error=14, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "create_relations" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // WRITE metadata
```

### open_nodes

**Class:** `metadata` · **Authority:** `read` · **Confidence:** 73.0%

**Description:** Open specific nodes in the knowledge graph by their names

**Rationale:** name match "\b(read[_-]?graph|search[_-]?nodes|open[_-]?nodes)\b" → metadata/read (0.70); fuzz: 1/15 accepted → +0.1

**Fuzz:** 15 calls — ok=1, protocol_error=14, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "open_nodes" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ metadata
```

### read_graph

**Class:** `metadata` · **Authority:** `read` · **Confidence:** 73.0%

**Description:** Read the entire knowledge graph

**Rationale:** name match "\b(read[_-]?graph|search[_-]?nodes|open[_-]?nodes)\b" → metadata/read (0.70); fuzz: 4/4 accepted → +0.1

**Fuzz:** 4 calls — ok=4, protocol_error=0, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "read_graph" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ metadata
```

### search_nodes

**Class:** `metadata` · **Authority:** `read` · **Confidence:** 73.0%

**Description:** Search for nodes in the knowledge graph based on a query

**Rationale:** name match "\b(read[_-]?graph|search[_-]?nodes|open[_-]?nodes)\b" → metadata/read (0.70); fuzz: 7/15 accepted → +0.1

**Fuzz:** 15 calls — ok=7, protocol_error=8, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "search_nodes" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ metadata
```

---

## How to read this report

Every classification has a `confidence` score (noisy-OR over fired rules — see [docs/METHODOLOGY.md](https://github.com/euanmcrosson-dotcom/mcp-recon/blob/master/docs/METHODOLOGY.md)) and a free-form `rationale` listing every rule that fired. If you disagree with a classification:

1. Read the `rationale` — which rules fired, and which you'd reweight.
2. Edit `crates/mcp-recon-core/src/rules/v0_1.rs` (rule weights) or `packages/mcp-recon-cli/src/classify/rules.ts` (TS mirror) and re-run.
3. Open an issue with your reasoning so future runs benefit.

**Recommended caveats are starting points, not finish lines.** Tighten the placeholders to match your deployment, and consider attaching additional caveats (caller binding, time-of-day windows, rate limits enforced upstream).
