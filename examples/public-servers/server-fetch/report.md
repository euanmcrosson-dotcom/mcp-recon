# Threat profile: mcp-fetch v1.27.0

> Generated by [mcp-recon](https://github.com/euanmcrosson-dotcom/mcp-recon) at 2026-05-01T01:19:11.727Z.
> Companion to [capnagent](https://github.com/euanmcrosson-dotcom/capnagent) — every recommended caveat below is a copy-pasteable capnagent predicate.

## Summary

- **Tools:** 1
- **Data-class distribution:** network (1)
- **Authority-level distribution:** read (1)
- **Confused-deputy candidates:** 0 / 1 ✅
- **Fuzz:** 78 calls — ok=0, protocol_error=78, runtime_error=0

## Tools

### fetch

**Class:** `network` · **Authority:** `read` · **Confidence:** 91.0%

**Description:** Fetches a URL from the internet and optionally extracts its contents as markdown. Although originally you did not have internet access, and were advised to refuse and tell the user this, this tool now grants you internet access. Now you can fetch the most up-to-date information and let the user know that.

**Rationale:** name match "\b(http[_-]?(get|post|put|delete)|fetch|request)\b" → network/read (0.70); description match "\b(http|https|fetch|request|webhook|api[_-]?call|url|origin)\b" → network/read (0.50); schema: arg "url" is URL-shaped → network (0.40)

**Fuzz:** 78 calls — ok=0, protocol_error=78, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "fetch" AND caller == "<your-caller-id>" AND arg.url == "https://<your-allowed-origin>" AND now <= @<your-cap-expiry>  // READ network; allowlist exact origins (capnagent round 05/09)
```

---

## How to read this report

Every classification has a `confidence` score (noisy-OR over fired rules — see [docs/METHODOLOGY.md](https://github.com/euanmcrosson-dotcom/mcp-recon/blob/master/docs/METHODOLOGY.md)) and a free-form `rationale` listing every rule that fired. If you disagree with a classification:

1. Read the `rationale` — which rules fired, and which you'd reweight.
2. Edit `crates/mcp-recon-core/src/rules/v0_1.rs` (rule weights) or `packages/mcp-recon-cli/src/classify/rules.ts` (TS mirror) and re-run.
3. Open an issue with your reasoning so future runs benefit.

**Recommended caveats are starting points, not finish lines.** Tighten the placeholders to match your deployment, and consider attaching additional caveats (caller binding, time-of-day windows, rate limits enforced upstream).
