# Threat profile: mcp-time v1.27.0

> Generated by [mcp-recon](https://github.com/euanmcrosson-dotcom/mcp-recon) at 2026-05-01T01:19:18.869Z.
> Companion to [capnagent](https://github.com/euanmcrosson-dotcom/capnagent) — every recommended caveat below is a copy-pasteable capnagent predicate.

## Summary

- **Tools:** 2
- **Data-class distribution:** unknown (1), filesystem (1)
- **Authority-level distribution:** read (2)
- **Confused-deputy candidates:** 0 / 2 ✅
- **Fuzz:** 132 calls — ok=0, protocol_error=132, runtime_error=0

## Tools

### convert_time

**Class:** `filesystem` · **Authority:** `read` · **Confidence:** 64.0%

**Description:** Convert time between timezones

**Rationale:** schema: arg "source_timezone" is path-shaped → filesystem (0.40); schema: arg "target_timezone" is path-shaped → filesystem (0.40)

**Fuzz:** 102 calls — ok=0, protocol_error=102, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "convert_time" AND caller == "<your-caller-id>" AND arg.source_timezone starts_with "<your-sandbox-prefix>/" AND arg.target_timezone starts_with "<your-sandbox-prefix>/" AND now <= @<your-cap-expiry>  // READ filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### get_current_time

**Class:** `unknown` · **Authority:** `read` · **Confidence:** 0.0%

**Description:** Get current time in a specific timezones

**Rationale:** no rules fired

**Fuzz:** 30 calls — ok=0, protocol_error=30, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "get_current_time" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ unclassified — operator must review
```

---

## How to read this report

Every classification has a `confidence` score (noisy-OR over fired rules — see [docs/METHODOLOGY.md](https://github.com/euanmcrosson-dotcom/mcp-recon/blob/master/docs/METHODOLOGY.md)) and a free-form `rationale` listing every rule that fired. If you disagree with a classification:

1. Read the `rationale` — which rules fired, and which you'd reweight.
2. Edit `crates/mcp-recon-core/src/rules/v0_1.rs` (rule weights) or `packages/mcp-recon-cli/src/classify/rules.ts` (TS mirror) and re-run.
3. Open an issue with your reasoning so future runs benefit.

**Recommended caveats are starting points, not finish lines.** Tighten the placeholders to match your deployment, and consider attaching additional caveats (caller binding, time-of-day windows, rate limits enforced upstream).
