# Threat profile: mcp-servers/everything v2.0.0

> Generated by [mcp-recon](https://github.com/euanmcrosson-dotcom/mcp-recon) at 2026-04-29T20:49:10.241Z.
> Companion to [capnagent](https://github.com/euanmcrosson-dotcom/capnagent) — every recommended caveat below is a copy-pasteable capnagent predicate.

## Summary

- **Tools:** 13
- **Data-class distribution:** metadata (9), system (3), filesystem (1)
- **Authority-level distribution:** read (9), write (4)
- **Confused-deputy candidates:** 1 / 13
- **Fuzz:** 151 calls — ok=51, protocol_error=99, runtime_error=1  ⚠️ at least one input escaped protocol-error handling

## Tools

### simulate-research-query

**Class:** `metadata` · **Authority:** `write` ⚠️ confused-deputy candidate · **Confidence:** 70.0%

**Description:** Simulates a deep research operation that gathers, analyzes, and synthesizes information. Demonstrates MCP task-based operations with progress through multiple stages. If 'ambiguous' is true and client supports elicitation, sends an elicitation request for clarification.

**Rationale:** description match "\b(http|https|fetch|request|webhook|api[_-]?call|url|origin)\b" → network/read (0.50); name match "\b(trigger[_-]?(long[_-]?running[_-]?operation)?|simulate[_-]?(research[_-]?query))\b" → metadata/write (0.70); user-controllable string arg + non-read authority → confused-deputy candidate

**Fuzz:** 15 calls — ok=0, protocol_error=15, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "simulate-research-query" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // WRITE metadata
```

### trigger-long-running-operation

**Class:** `metadata` · **Authority:** `write` · **Confidence:** 73.0%

**Description:** Demonstrates a long running operation with progress updates.

**Rationale:** name match "\b(trigger[_-]?(long[_-]?running[_-]?operation)?|simulate[_-]?(research[_-]?query))\b" → metadata/write (0.70); fuzz: 10/15 accepted → +0.1

**Fuzz:** 15 calls — ok=10, protocol_error=4, runtime_error=1  ⚠️

**Recommended capnagent caveat:**

```
tool == "trigger-long-running-operation" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // WRITE metadata
```

### toggle-simulated-logging

**Class:** `system` · **Authority:** `write` · **Confidence:** 73.0%

**Description:** Toggles simulated, random-leveled logging on or off.

**Rationale:** name match "\b(toggle[_-]?(simulated[_-]?logging|subscriber[_-]?updates))\b" → system/write (0.70); fuzz: 4/4 accepted → +0.1

**Fuzz:** 4 calls — ok=4, protocol_error=0, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "toggle-simulated-logging" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // WRITE system; usually safe but verify env-var exposure
```

### toggle-subscriber-updates

**Class:** `system` · **Authority:** `write` · **Confidence:** 73.0%

**Description:** Toggles simulated resource subscription updates on or off.

**Rationale:** name match "\b(toggle[_-]?(simulated[_-]?logging|subscriber[_-]?updates))\b" → system/write (0.70); fuzz: 4/4 accepted → +0.1

**Fuzz:** 4 calls — ok=4, protocol_error=0, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "toggle-subscriber-updates" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // WRITE system; usually safe but verify env-var exposure
```

### gzip-file-as-resource

**Class:** `filesystem` · **Authority:** `read` · **Confidence:** 55.0%

**Description:** Compresses a single file using gzip compression. Depending upon the selected output type, returns either the compressed data as a gzipped resource or a resource link, allowing it to be downloaded in a subsequent request during the current session.

**Rationale:** description match "\b(file|directory|folder|filesystem|filepath|dirent)\b" → filesystem/read (0.50); description match "\b(http|https|fetch|request|webhook|api[_-]?call|url|origin)\b" → network/read (0.50); fuzz: 6/15 accepted → +0.1

**Fuzz:** 15 calls — ok=6, protocol_error=9, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "gzip-file-as-resource" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ filesystem; bound the sandbox prefix tightly (capnagent round 07/10)
```

### echo

**Class:** `metadata` · **Authority:** `read` · **Confidence:** 73.0%

**Description:** Echoes back the input string

**Rationale:** name match "\b(echo|get[_-]?(structured[_-]?content|annotated[_-]?message|resource[_-]?(links|reference)|sum|tiny[_-]?image))\b" → metadata/read (0.70); fuzz: 7/15 accepted → +0.1

**Fuzz:** 15 calls — ok=7, protocol_error=8, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "echo" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ metadata
```

### get-annotated-message

**Class:** `metadata` · **Authority:** `read` · **Confidence:** 70.0%

**Description:** Demonstrates how annotations can be used to provide metadata about content.

**Rationale:** name match "\b(echo|get[_-]?(structured[_-]?content|annotated[_-]?message|resource[_-]?(links|reference)|sum|tiny[_-]?image))\b" → metadata/read (0.70)

**Fuzz:** 15 calls — ok=0, protocol_error=15, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "get-annotated-message" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ metadata
```

### get-resource-links

**Class:** `metadata` · **Authority:** `read` · **Confidence:** 70.0%

**Description:** Returns up to ten resource links that reference different types of resources

**Rationale:** name match "\b(echo|get[_-]?(structured[_-]?content|annotated[_-]?message|resource[_-]?(links|reference)|sum|tiny[_-]?image))\b" → metadata/read (0.70)

**Fuzz:** 15 calls — ok=0, protocol_error=15, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "get-resource-links" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ metadata
```

### get-resource-reference

**Class:** `metadata` · **Authority:** `read` · **Confidence:** 73.0%

**Description:** Returns a resource reference that can be used by MCP clients

**Rationale:** name match "\b(echo|get[_-]?(structured[_-]?content|annotated[_-]?message|resource[_-]?(links|reference)|sum|tiny[_-]?image))\b" → metadata/read (0.70); fuzz: 1/15 accepted → +0.1

**Fuzz:** 15 calls — ok=1, protocol_error=14, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "get-resource-reference" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ metadata
```

### get-structured-content

**Class:** `metadata` · **Authority:** `read` · **Confidence:** 70.0%

**Description:** Returns structured content along with an output schema for client data validation

**Rationale:** name match "\b(echo|get[_-]?(structured[_-]?content|annotated[_-]?message|resource[_-]?(links|reference)|sum|tiny[_-]?image))\b" → metadata/read (0.70)

**Fuzz:** 15 calls — ok=0, protocol_error=15, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "get-structured-content" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ metadata
```

### get-sum

**Class:** `metadata` · **Authority:** `read` · **Confidence:** 73.0%

**Description:** Returns the sum of two numbers

**Rationale:** name match "\b(echo|get[_-]?(structured[_-]?content|annotated[_-]?message|resource[_-]?(links|reference)|sum|tiny[_-]?image))\b" → metadata/read (0.70); fuzz: 11/15 accepted → +0.1

**Fuzz:** 15 calls — ok=11, protocol_error=4, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "get-sum" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ metadata
```

### get-tiny-image

**Class:** `metadata` · **Authority:** `read` · **Confidence:** 73.0%

**Description:** Returns a tiny MCP logo image.

**Rationale:** name match "\b(echo|get[_-]?(structured[_-]?content|annotated[_-]?message|resource[_-]?(links|reference)|sum|tiny[_-]?image))\b" → metadata/read (0.70); fuzz: 4/4 accepted → +0.1

**Fuzz:** 4 calls — ok=4, protocol_error=0, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "get-tiny-image" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ metadata
```

### get-env

**Class:** `system` · **Authority:** `read` · **Confidence:** 86.5%

**Description:** Returns all environment variables, helpful for debugging MCP server configuration

**Rationale:** name match "\b(get[_-]?env|environ(ment)?|process[_-]?(info|status)|sysinfo|system[_-]?info|hostname)\b" → system/read (0.70); description match "\b(get[_-]?env|environ(ment)?|process[_-]?(info|status)|sysinfo|system[_-]?info|hostname)\b" → system/read (0.50); fuzz: 4/4 accepted → +0.1

**Fuzz:** 4 calls — ok=4, protocol_error=0, runtime_error=0

**Recommended capnagent caveat:**

```
tool == "get-env" AND caller == "<your-caller-id>" AND now <= @<your-cap-expiry>  // READ system; usually safe but verify env-var exposure
```

---

## How to read this report

Every classification has a `confidence` score (noisy-OR over fired rules — see [docs/METHODOLOGY.md](https://github.com/euanmcrosson-dotcom/mcp-recon/blob/master/docs/METHODOLOGY.md)) and a free-form `rationale` listing every rule that fired. If you disagree with a classification:

1. Read the `rationale` — which rules fired, and which you'd reweight.
2. Edit `crates/mcp-recon-core/src/rules/v0_1.rs` (rule weights) or `packages/mcp-recon-cli/src/classify/rules.ts` (TS mirror) and re-run.
3. Open an issue with your reasoning so future runs benefit.

**Recommended caveats are starting points, not finish lines.** Tighten the placeholders to match your deployment, and consider attaching additional caveats (caller binding, time-of-day windows, rate limits enforced upstream).
